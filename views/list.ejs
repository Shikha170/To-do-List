<%- include("header") -%>
        <div class="box" id="heading">
        <h1>hey it's <%=ListTitle %>!</h1>
        </div> 
        <div class="box">
           
                <!-- <li><%=newListItem %></li> -->
               <!-- <% for(var i=0;i<newListItem.length;i++){%>
                <div class="item">
                    <input type="checkbox">
                    <p><%= newListItem[i].name %></p>
                </div>
               <%}%> -->

               <%newListItem.forEach(function(item){%>
                <form action="/delete" method="post"class="">
                    <div class="item">
                        <input type="checkbox" name="checkbox" value=" <%=item._id%>"onChange="this.form.submit()">
                        <p><%= item.name %></p>
                        
                    </div>
                    <input type="hidden" name="listName" value="<%=ListTitle%>"></input>
                </form>
                <%})%>
               <!-- <% newListItem.forEach(item => { %>
                <div class="item">
                    <input type="checkbox">
                    <p><%= item.name %></p>
                </div>
            <% }); %> -->
               <form class="item"action="/" method="post"class="">
                <input type="text"name="newItem"placeholder="Add New Item"autocomplete="off">
                <button type="submit"name="list"value="<%=ListTitle %>">+</button>
            </form>
        </div>
 <%- include("footer") -%>
            
    
    
